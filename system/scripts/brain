#!/usr/bin/env bash
# brain — wrapper executável da CLI do Mneme.
# Uso: brain status | brain remember "..." | brain context project-x
set -euo pipefail
HERE="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
exec python3 "${HERE}/brain.py" "$@"
